#!/bin/sh
# Everything quick before a commit: formatting, types, lint and the unit tests.
# The gates take longer; run `npm run check` for those too.
# Skip in an emergency with `git commit --no-verify`.
exec npm run --silent check:quick
